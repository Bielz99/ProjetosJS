// Crie um array
// Imprima cada palavra do array no console por meio de um for

let frase = "Ola seja bem vindo";
let arrFrase = frase.split(" ");

for (let index = 0; index < frase.length; index++) {
  console.log(arrFrase[index]);
}
