// Escreva uma funcao que receba dois numeros, o primeiro é a base e o segundo a potencia
// Depois faça essa operacao e retorne o resultado";
// Por exemplo: 3,2 =9";

function calcularPotencia(x, y) {
  return Math.pow(x, y);
}

console.log(`O resultado da operacao é ${calcularPotencia(3, 2)}`);
